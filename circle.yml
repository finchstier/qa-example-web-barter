#version: 2
#jobs:
#  build:
#    environment:
#      TEST_REPORTS: /tmp/target
#
#    working_directory: ~/tmp/qa-example-web-barter
#
##    branches:
#
#    steps:
#      - checkout
#
#      - run:
#          environment:
#            SSH_TARGET: "localhost"
#            TEST_ENV: "linux"
#          command: |
#            set -xu
#            mkdir -p ${TEST_REPORTS}
#            run-tests.sh
#            cp out/tests/*.json ${TEST_REPORTS}
#
#      - run: |
#          set -xu
#          mkdir -p /tmp/artifacts
#          create_jas.sh ${CIRCLE_BUILD_NUM}
#          cp *.jar /tmp/artifacts
#
#      # Save artifacts
#      - store_artifacts:
#          path: /tmp/artifacts
#          destination: build
#
#      # Upload test results
#      - store_test_results:
#          path: /tmp/target
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/cucumber
    - bundle exec cucumber --format pretty --format json --out $CIRCLE_TEST_REPORTS/cucumber/tests.cucumber